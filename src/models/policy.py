# Abstract the LLM here into a general policy model
# This class will provide the API for generate() which takes in the conversation context and outputs responses and log_probabilities
# and forward() which takes inputs and labels, and generates logits/log_probabilities
